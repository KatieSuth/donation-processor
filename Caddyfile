donation-processor.localhost {
    # Backend
    handle /api/* {
        uri strip_prefix /api
        reverse_proxy api:8080
    }

    # Frontend
    handle {
        reverse_proxy frontend:3000
    }
}